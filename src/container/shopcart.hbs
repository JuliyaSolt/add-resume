<main>  
    {{#with header}}
    <h1>{{title}}</h1> 
    <a class="btn btn-outline-secondary mb-2" href="{{link}}">{{button.text}}</a>
    {{/with}}
               
     <hr class="my-5">
                             
    {{!-- {{#each goodsBlock}}
    <div class="card my-4">
      <div class="container">
        <div class="row">
          <div class="col-2">
            <img src="https://picsum.photos/110/110" alt="Product photo">           
          </div>

          <div class="col-4">
            <h6>{{name}}</h6>
            <p>{{description}}</p>
          </div>

          <div class="col-2 py-5">
            {{price.title}}:
            <p>{{price.value}}</p>
          </div>

          <div class="col-1 py-5">
              {{amount}}
          </div>
            
          <div class="col-3 text-center py-4">
            {{#each buttons}}
              {{#if isPrimary}}<button class="btn text-bg-primary">{{text}}</button>{{/if}} 
              {{#if isDanger}}<button class="btn btn-outline-danger">{{text}}</button>{{/if}}</p> 
            {{/each}}
          </div>
        </div> 
      </div>         
    </div>
  {{/each}} --}}
  {{> cartGoodsItem}}
               <hr class="my-5">
               <div class="col-12">
                {{#with total}}
                <h2>{{title}}</h2>
                <p>{{amount}}</p>
                <p>{{delivery}}</p>

                {{#each buttons}}
                {{#if isSuccess}}<button class="btn text-bg-success">{{text}}</button>{{/if}} 
                {{#if isOutline}}<button class="btn btn-outline-success">{{text}}</button>{{/if}}</p> 
                {{/each}}
                {{/with}}
               </div>
               <hr class="my-5">
               {{> shopcard}}
              <hr class="my-5">
                
         <div class="col-12 my-2">
            <div class="row gy-3 gx-3">
               <div class="card text-bg-dark">
               {{#with subscribe}}
               <h2>{{header}}</h2>
               <p class="pb-4">{{description}}</p>
               
               
               <div class="text-end pe-2 pb-3">
               {{#each buttons}}
               <a href={{link}} class="btn btn-primary text-bg-primary m-2 ms-4">{{text}}</span></a>
               {{/each}}
               {{/with}}
               </div>
               </div>
            </div>
         </div>
	     <hr class="my-5">    
</main>
    